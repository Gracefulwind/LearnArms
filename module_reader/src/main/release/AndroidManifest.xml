<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gracefulwind.learnarms.reader">

    <application>

        <activity
            android:name=".mvp.ui.activity.MainActivity"
            android:screenOrientation="portrait" /> <!-- 在组件集成宿主时, 由于宿主已经声明了 CommonSDK 的公有 ConfigModule, 所以在这里只需要声明业务组件自己的 ConfigModule -->
        <activity android:name=".mvp.ui.activity.DoodleActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.HandWriteActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.TestGestureActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.HandNoteActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.TestTextActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.ReaderMainActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".mvp.ui.activity.BookReadActivity"
            android:screenOrientation="portrait"
            />
        <!-- 在组件集成宿主时, 由于宿主已经声明了 CommonSDK 的公有 ConfigModule, 所以在这里只需要声明业务组件自己的 ConfigModule -->
        <meta-data
            android:name="com.gracefulwind.learnarms.reader.app.GlobalConfiguration"
            android:value="ConfigModule" />
    </application>

</manifest>